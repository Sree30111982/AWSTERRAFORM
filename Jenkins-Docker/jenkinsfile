pipeline {
  agent { label windows } 
  {
    docker { image 'node:16-alpine' }
  }

  stages {
        stage('Test') {
            steps {
                echo "Running on Windows Node"
            }
 }
  }